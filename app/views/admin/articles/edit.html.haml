- @page_title = 'ニュース記事の編集'
%h1= @page_title

%p= link_to '記事の詳細に戻る', admin_article_path(@article)

= form_with model: [:admin, @article], local: true, class: 'article-form' do |form|
  = render 'form', form: form
  .article-form-submit= form.submit
